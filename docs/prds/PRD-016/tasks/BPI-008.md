# BPI-008 — Complete software regression and resource evidence

Status: Software Complete — Target Pending
Review Mode: Agent
Review Reason: Configured tests, typechecks, sanitizers, contract captures,
resource assertions, and an available pinned target build produce objective
software evidence.

## Goal

Run the complete affected verification matrix in both controller build modes,
fix regressions, and publish exact resource/timing/build evidence or exact
environment limitations.

## Scope

- Run OpenAPI/protocol, simulator, mobile, desktop, firmware native/sanitizer,
  capture, and resource checks.
- Build/test firmware host configurations with PI disabled and enabled.
- Run the pinned ESP-IDF 6.0.2 target build and relevant size/map checks when
  the configured environment is available.
- Rebaseline history entry/ring/serialization, stack/heap/flash, and control-loop
  deadline evidence.
- Update task files, PRD acceptance state, and tracker with exact commands and
  results.

## Non-Scope

- Installing missing tools, connected hardware work, energized testing,
  physical thermal acceptance, commits, pushes, branches, or pull requests.

## Implementation Plan

1. Record the exact source/config identity and verification commands.
2. Run every affected TypeScript/Python/C++ configured check.
3. Run both controller-mode host matrices and the available target build.
4. Fix software regressions and repeat affected/full checks.
5. Publish resource evidence, unavailable command details, compatibility and
   safety impact, and remaining Human gates.

## Acceptance Criteria

- [x] All available affected workspace tests/typechecks/lint pass.
- [x] Protocol schemas, fixtures, OpenAPI, simulator, and firmware captures
  agree.
- [x] Firmware native and sanitizer suites pass with PI disabled and enabled.
- [x] History/resource evidence remains within approved budgets and no software
  evidence shows a 500 ms deadline, watchdog, or lease regression.
- [x] The pinned target build passes if configured; otherwise the exact unrun
  command and missing environment prerequisite are recorded.
- [x] PRD/task/tracker evidence distinguishes software from physical acceptance.

## Completion Evidence

- PASS — OpenAPI; protocol 138 tests / 280 expectations and typecheck;
  simulator 77 / 556 and typecheck; mobile 180 / 1,188, typecheck, and Expo
  lint; desktop 32 tests.
- PASS — native 10/10, sanitizer 10/10, separate legacy/PI authority
  executables, exact resource output, and 32 strict firmware captures.
- `idf.py` is unavailable in the configured shell, so the pinned ESP-IDF 6.0.2
  build, target map/size, stack, heap/flash delta, and connected timing remain
  pending. No installation was attempted.
- Full commands, exact resource values, compatibility, and evidence boundaries:
  `../evidence/SOFTWARE-VERIFICATION.md`.

## Verification Strategy

- Full repository verification matrix limited to existing tools/dependencies,
  with exact pass counts and commands recorded under PRD-016 evidence.

## Dependencies

- BPI-001 through BPI-007 complete.

## Files Expected To Change

- `docs/prds/PRD-016/PRD-016.md`
- `docs/prds/PRD-016/tasks/`
- `docs/prds/PRD-016/evidence/`
- `docs/TRACKER.md`
- only source/test files required to fix discovered in-scope regressions
